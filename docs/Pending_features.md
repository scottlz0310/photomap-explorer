# 🚧 将来の拡張機能

## 基本操作の改善
- アドレスバーで直接フォルダパス入力が可能に。（実装済2025.06.24）フォルダジャンプ機能により廃止
- フォルダペインはツリー表示ではなく、パスに応じてカレントフォルダ直下のみをリスト表示する方式に変更（実装済2025.06.26）
- フォルダビューでフォルダをダブルクリックすると、そのフォルダに移動しアドレスバーも更新されるようにする（実装済2025.06.26）
- アドレスバーのフォルダをダブルクリックで対象フォルダに移動する（実装済2025.06.25）
- 「全ドライブに戻る」ボタンは「⬆️（親フォルダへ移動）」ボタンとして動作するように変更（実装済2025.06.26）
- CTRLクリックによる複数選択を実装し、左右カーソルキーや⬅️➡️ボタンによる選択画像の比較機能を追加
- 戻るボタンのUI見直し（フォルダペインの最上段に配置し、視線誘導を自然にする）（実装済2025.06.26）
- アドレスバーの区切り文字をなくしてボタンに枠を追加して可視化します。

## 表示機能の強化
- サムネイルのサイズ変更（大・中・小）を表示メニューで調整可能に。(v2.0で失われた機能、新UIで再実装予定)
- 全画面表示トグルをダブルクリックで切り替えられる。全画面ボタンとデフォルト表示に戻るボタンを設置。（実装済2025.06.26）
- ステータスバーに画像解像度、撮影日時、撮影情報（カメラ、シャッタースピード等）を統合表示。（実装済2025.06.26）
- ファイルやサムネイルエリアの並び順を簡単に切り替えられるようにする。そしてその順番は同期させる。（実装済2025.06.26）
- スライドスイッチによるダークモードの実装。スタイルシート使用。（実装済2025.06.28 v2.1.0）
- 中央ペインの最小幅はサムネイルサイズの値に連動するようにする（サムネイルサイズ変更機能実装後）

## ドラッグ＆ドロップ機能
- フォルダ選択・画像登録をドラッグ＆ドロップで簡略化。起動引数処理も参照。
- 他アプリへCTRL+CLKで選択した複数画像をドラッグ＆ドロップで他アプリに渡す。フォルダ移動も兼ねる。

## 最近の履歴管理
- 開いたフォルダ履歴をメニューやサイドバーで表示し、迅速なアクセスを提供。UI検討中

## 右クリックメニューの追加
- サムネイルやファイル選択部分に右クリックメニューを追加し、操作を簡素化。
    プロパティ、削除、切り取り、コピー、等
- フォルダの作成や画像のコピー、移動、削除などを可能に。
    右クリックメニューと同義

## AI活用と画像フィルタリング
- 画像の特徴（色、構図、被写体）を基にAIが似た画像を選別。
    マスキングエリアを設定し、周辺部重視で似た画像をピックアップするなど。
- ジオタグを活用した撮影位置範囲の絞り込みとマップ表示の強化。
    MAPをドラッグで選択し、範囲内の画像を自動で選択状態に。

## 追加機能の検討
- メニューバーの導入（ファイル・表示・設定など基本構成）。
- 起動引数を利用してフォルダ/ファイルへの自動遷移を実現。

## 第2回コードリファクタリング計画

✅ **完了** - 2025年6月末時点でClean Architectureベースの新構造が実装済み

## 第3回コードリファクタリング計画

### 🚨 最重要課題: メガファイル問題
- **`presentation/views/functional_new_main_view.py`** (1690行) - 極度に肥大化した単一ファイル
- **`ui/controls.py`** (425行) - UIコントロールの集中
- **MainWindow実装重複** - `window/main_window.py` との二重実装

### 🎯 第3回リファクタリング目標
1. **メガファイル分割**: 1690行 → 8-10個の200-300行ファイル群
2. **実装統一**: 重複するMainWindow実装の整理・統合
3. **テスタビリティ向上**: 単体テスト可能な構造への変更
4. **保守性向上**: 単一責任原則の徹底適用

### 📋 実施フェーズ
- **Phase 1**: `functional_new_main_view.py` の分割 (最優先)
- **Phase 2**: MainWindow実装の統一
- **Phase 3**: `ui/controls.py` の分割
- **Phase 4**: 基本テストスイートの構築
- **Phase 5**: パフォーマンス最適化・既知不具合修正

詳細: `docs/THIRD_REFACTORING_PLAN.md`, `docs/THIRD_REFACTORING_ANALYSIS.md`

## 既知の不具合

- [2025-06-27] アドレスバーのボタンが表示しきれなくなるとウィンドウ全体が拡大して表示される。アドレスバーが狭いときは、ルート側から順にボタンを隠し、末尾（子階層）を優先表示する。
- [2025-06-27] ステータスバーの表示が時間で消えてしまう。(fix済2025-06-28)
- [2025-06-27] 画像が多いフォルダで読み込みが遅い。サムネイル非同期化検討中。(fix済2025-06-28)
- [2025-06-27] スライドバーを動かすとき、画像の大きさが自動でフィットしない。
- [2025-06-30] 全体表示ボタンが小さくて見にくい。アイコンのみで分かりにくいのでマウスオンで説明
- [2025-06-30] フォルダ選択ウィンドウにはファイル名しか表示されていないので、サイズと更新日時を追加しそれぞれソート可能にする
- [2025-06-30] ライト・ダークモード切り替え時に画面内が一瞬揺れて見える。初期化作業のチェックを
- [2025-06-30] フォルダ選択ウィンドウでクリックした場合、下階層のサムネイルが表示されない（表示されると横断的に目的の画像を探せる）(fix済2025-06-30)
- [2025-06-30] 上記fixにより、子階層のサムネイルをダブルクリックしたら、その子階層に移動しないので移動するように。
- [2025-06-30] 画像ビュー画面に(ペインでも全画面でも)◀左右▶ボタンの配置で画像切替、左右カーソルキーでも同様の操作
- [2025-06-30] サムネイルエリアにサムネイルサイズの切り替え。ラジオボタンでプリセットとスライドバーで無段階調整。
